name: 'pysen-test'
description: 'Run pysen test'
runs:
  using: 'docker'
  image: 'Dockerfile'
  entrypoint: '/bin/bash'
  args:
    - '-c'
    - "git config --global --add safe.directory * && git diff --abbrev=40 --full-index -M --raw -z --no-color && tox -e development"
